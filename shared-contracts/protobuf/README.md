# protobuf (Reserved)

Reserved for future gRPC contracts.

Planned examples:

- `planner.proto`: request/response for optimized plan generation
- `tutor_stream.proto`: bidirectional stream contract for tutoring chunks
- `learning_event.proto`: async event envelope for graph updates

Current status: no `.proto` files yet (architecture-only stage).
