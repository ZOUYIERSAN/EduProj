# Architecture-only Docker scaffold for ai-engine-python.

FROM python:3.11-slim

WORKDIR /app

# NOTE:
# Real dependency install and startup command will be added later.
# This file exists to reserve deployment and local parity workflow.
